{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell exposes event IPC only: it forwards the native 文件 menu actions to the webview as `desktop-menu` events; the harness GUI talks to the managed dsh web server over HTTP/WebSocket for everything else.",
  "windows": ["main"],
  "permissions": [
    "core:event:default"
  ]
}
