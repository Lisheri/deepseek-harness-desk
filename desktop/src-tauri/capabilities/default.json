{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell exposes event and drag IPC only: it forwards the native 文件 menu actions to the webview as `desktop-menu` events, and the titlebar's data-tauri-drag-region area drags the window (core:window:allow-start-dragging). The harness GUI talks to the managed dsh web server over HTTP/WebSocket for everything else.",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "core:window:allow-start-dragging"
  ]
}
